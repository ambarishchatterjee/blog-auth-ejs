<nav class="bg-white shadow-md">
  <div class="container mx-auto flex justify-between items-center px-4 py-3">
    <!-- Logo -->
    <a href="/dashboard" class="text-2xl font-bold text-indigo-600 hover:text-indigo-700 transition">Blogify</a>

    <!-- Links -->
    <div class="flex items-center space-x-4">

      <% if (!user) { %>
        <a href="/login" class="text-gray-700 hover:text-indigo-600 transition">Login</a>
        <a href="/register" class="text-gray-700 hover:text-indigo-600 transition">Register</a>
      <% } else { %>
        <a href="/dashboard" class="text-gray-700 hover:text-indigo-600 transition">Dashboard</a>

        <% if (user.isAdmin) { %>
          <a href="/admin/dashboard" class="text-gray-700 font-semibold hover:text-indigo-600 transition">
            Admin Panel
          </a>
        <% } %>

        <a href="/api/auth/logout">
          <button type="submit" class="text-red-500 hover:text-red-700 transition">Logout</button>
        </a>
      <% } %>
    </div>
  </div>
</nav>
