<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %> | Blog App</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">
  <nav class="bg-blue-600 p-4 text-white flex justify-between">
    <a href="/blogs" class="font-bold">My Blog</a>
    <div>
      <% if (user) { %>
        <a href="/dashboard" class="px-2">Dashboard</a>
        <a href="/blogs/create" class="px-2">New Blog</a>
        <a href="/api/auth/logout" class="px-2">Logout</a>
      <% } else { %>
        <a href="/login" class="px-2">Login</a>
        <a href="/register" class="px-2">Register</a>
      <% } %>
    </div>
  </nav>
  <main class="container mx-auto p-6">
